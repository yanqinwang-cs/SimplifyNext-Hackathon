# Blind adversary task

Read only the supplied frozen public package. Generate plausible, subtle outputs that appear close to compliant but test ambiguity, under-specification, unavailable references, mixed branches, copied template text, and cross-field inconsistencies. Do not inspect implementation, tests, mutation fixtures, prior results, or validator behavior. Persist exact raw outputs for later evaluation.

If repository or implementation access is available, the batch is `NOT_BLIND` and must not contribute to blind resistance statistics.
