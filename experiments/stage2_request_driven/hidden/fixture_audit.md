# Stage 2B hidden fixture audit

The hidden fixture keys, matcher concepts, and expected controlled-release order are environment-only audit material. They must never be rendered into an Investigator prompt or public workspace.

`deterministic_timeline.md` is intentionally not part of the releasable pool.
